[project]
name = "oauth-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.1",
    "fastmcp>=2.13.0.1",
    "httpx>=0.28.1",
    "ruff>=0.14.2",
]
